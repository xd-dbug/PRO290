import { getProfile, updateProfile, deleteAccount } from './user'

describe('user', () => {
  it.todo('getProfile calls GET /api/users/me with token injected')
  it.todo('updateProfile calls PATCH /api/users/me with correct body')
  it.todo('deleteAccount calls DELETE /api/users/me')
})