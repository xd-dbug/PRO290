import { getNotes, createNote, updateNote, deleteNote } from './notes'

describe('notes', () => {
  it.todo('getNotes calls GET /api/users/me/notes')
  it.todo('createNote calls POST /api/users/me/notes with title and body')
  it.todo('updateNote calls PATCH /api/users/me/notes/:noteId with correct body')
  it.todo('deleteNote calls DELETE /api/users/me/notes/:noteId')
})